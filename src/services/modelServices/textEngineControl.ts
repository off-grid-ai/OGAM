import { textEngineControl } from '../composition/text-engine';

/** The one text-engine control application, composed in `../composition/text-engine`. */
export const mobileTextEngineControl = textEngineControl();
