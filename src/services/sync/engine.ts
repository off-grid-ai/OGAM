// Pointer only: the engine is composed in src/services/composition/sync-engine.ts. Kept while
// __tests__/pro/sync/** (another repo's tests) import this path.
export { buildSyncEngine,  } from '../composition/sync-engine';
